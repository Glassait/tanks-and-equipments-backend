spring:
    application:
        name: "FOLD_ BACKEND"
    profiles:
        active: "actuator,bdd,cache"

debug: true

logging:
    level:
        org:
            springframework: INFO
            hibernate: WARN
        com.glassait.tanks_and_equipments: DEBUG

server:
    port: 8080
