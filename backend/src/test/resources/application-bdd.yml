spring:
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
    datasource:
        password: "root"
        username: "root"
        url: "jdbc:mysql://localhost"
        hikari:
            maximum-pool-size: 5
